import * as types from "./Calculator"

/**
 * Student Name
 * @type {string} -Name
 * 
 */

const StudentName = "Anil";


/**
 *  array of Grades   
 * @type {Array<number>} -Number
 */

const Grades =[11, 33]


/**
 * @type{{
 *  id:number | string,
 *  name:string
 * }}
 */

const todo ={
    id:2,
    name:"ss"
}

/**
 * Calculate Tax 
 * @param {number} amount  -Price
 * @param {string} Name  - Name
 * @param {string} text  - Text
 * @returns {boolean} -total price
 */

const Total =() =>{

}


/**
 * student
 * @typedef {object} Student
 * @property {number} id --Student İd
 * @property {string} StudentName - Name
 * @property {Array<number>} StudentGrades - Grades
 */

/**
 * @type {Student}
 */


/**
 * Keylock Adresleri
 * @typedef {object} keyklock
 * @property {string} keyklock_Base_url
 */


/**
 * @type {keyklock}
 */

const test ={
    keyklock_Base_url
}


/**
 * @type {types.test}
 */
const test12={
    name:"ss",
    age:1
}